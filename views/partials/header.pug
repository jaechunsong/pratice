header.header
    .header_column
        form(action=routes.seach, method="get")
            input(type="text", placeholder="검색할 제목을 입력하세요", name='term')
    .header_column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) LogIn
